//扩展块
apply from : "../config.gradle"
ext {
    //自定义lib架包
    IPublic = [
            'weight' : ':lib_weight',
            'manager': ':lib_manager'
    ]
}

dependencies {
    IPublic.each { k, v -> implementation project(path: v)}
}